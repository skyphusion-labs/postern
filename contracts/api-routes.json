{
  "$comment": [
    "GENERATED by `npm run routes:emit` from inbound/src/routes.ts. Do not edit by hand:",
    "inbound/route-table.test.ts fails when this file and the source disagree.",
    "The worker's HTTP route table (#417/#449). One place where a route, its method,",
    "and the token scope it demands are written down for every seam: the worker",
    "validates itself against it (inbound/route-contract.test.ts drives the REAL",
    "handleApi), and each client asserts the paths it emits are paths the worker",
    "serves. Before this, every suite mocked its own idea of the worker, which is how",
    "the published clients drifted a feature generation behind with green tests.",
    "Adding a route means adding a row to inbound/src/routes.ts and re-emitting.",
    "scope: what requiredScope() demands; null means the route is not behind the bearer",
    "scope gate at all, and `auth` says what guards it instead.",
    "match: exact = the whole path; prefix = this path plus anything under it."
  ],
  "version": 1,
  "routes": [
    {
      "id": "health",
      "method": "GET",
      "path": "/health",
      "match": "exact",
      "scope": null,
      "auth": "public"
    },
    {
      "id": "root",
      "method": "GET",
      "path": "/",
      "match": "exact",
      "scope": null,
      "auth": "public"
    },
    {
      "id": "webmail",
      "method": "GET",
      "path": "/webmail",
      "match": "exact",
      "scope": null,
      "auth": "public",
      "note": "the single-page human door; the page then calls the API with its own session"
    },
    {
      "id": "mta-sts",
      "method": "GET",
      "path": "/.well-known/mta-sts.txt",
      "match": "exact",
      "scope": null,
      "auth": "public"
    },
    {
      "id": "smtp-auth",
      "method": "POST",
      "path": "/api/smtp-auth",
      "match": "exact",
      "scope": null,
      "auth": "smtp-credential"
    },
    {
      "id": "session",
      "method": "ANY",
      "path": "/api/session",
      "match": "exact",
      "scope": null,
      "auth": "session-mint"
    },
    {
      "id": "session-refresh",
      "method": "ANY",
      "path": "/api/session/refresh",
      "match": "exact",
      "scope": null,
      "auth": "session-cookie"
    },
    {
      "id": "ingest",
      "method": "POST",
      "path": "/ingest",
      "match": "exact",
      "scope": null,
      "auth": "transport-token",
      "note": "inbound mail ingestion, gated by POSTERN_TRANSPORT_TOKEN, never a mailbox token"
    },
    {
      "id": "send",
      "method": "POST",
      "path": "/api/send",
      "match": "exact",
      "scope": "send",
      "auth": "bearer",
      "note": "attachments are base64 over JSON (#70); forwardMessageId quotes a stored message, recipients stay caller-selected"
    },
    {
      "id": "send-alias",
      "method": "POST",
      "path": "/send",
      "match": "exact",
      "scope": "send",
      "auth": "bearer",
      "note": "back-compat alias of /api/send: the same handler, so deliberately the same list"
    },
    {
      "id": "reply",
      "method": "POST",
      "path": "/api/reply",
      "match": "exact",
      "scope": "send",
      "auth": "bearer",
      "note": "mode reply|replyAll derives the original recipients server-side (#363)"
    },
    {
      "id": "messages-seen",
      "method": "POST",
      "path": "/api/messages/seen",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "`for` writes a per-recipient override; under a session it must be the session identity (#410)"
    },
    {
      "id": "messages-flags",
      "method": "POST",
      "path": "/api/messages/flags",
      "match": "exact",
      "scope": "read",
      "auth": "bearer"
    },
    {
      "id": "messages-move",
      "method": "POST",
      "path": "/api/messages/move",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "mailbox archive|trash|junk, or null to restore the direction-default view"
    },
    {
      "id": "drafts",
      "method": "ANY",
      "path": "/api/drafts",
      "match": "prefix",
      "scope": "send",
      "auth": "bearer",
      "requireSeparator": true,
      "template": "/api/drafts/{id}[/send|/attachments[/{attachmentId}]]",
      "note": "list/create/update/delete/send plus draft attachments. Identity-owned: a static operator token gets E_IDENTITY_REQUIRED. PUT on an EXISTING draft requires its current updatedAt; an absent one is a conflict too, so an edit is read-modify-write"
    },
    {
      "id": "imap-drafts",
      "method": "ANY",
      "path": "/api/imap/drafts",
      "match": "prefix",
      "scope": "imap",
      "auth": "bearer",
      "requireSeparator": true,
      "template": "/api/imap/drafts/{id}",
      "note": "the door has no ambient identity, so it asserts the already-authenticated one on every call"
    },
    {
      "id": "imap-import",
      "method": "POST",
      "path": "/api/imap/import",
      "match": "exact",
      "scope": "imap",
      "auth": "bearer",
      "note": "folder sent|archive|trash|junk; rawMime is base64, refused 413 E_PAYLOAD_TOO_LARGE (before parse) past 22 MiB decoded (#493)"
    },
    {
      "id": "imap-roles",
      "method": "GET",
      "path": "/api/imap/roles",
      "match": "exact",
      "scope": "imap",
      "auth": "bearer",
      "note": "role membership is single-sourced on the Worker (#438): the door reads this map per login instead of parsing its own mirror var. Same projection as the operator /api/roles, gated on the least-privilege door token"
    },
    {
      "id": "admin-smtp-credentials",
      "method": "POST",
      "path": "/api/admin/smtp-credentials",
      "match": "exact",
      "scope": "admin",
      "auth": "bearer"
    },
    {
      "id": "admin-smtp-credential-delete",
      "method": "DELETE",
      "path": "/api/admin/smtp-credentials/",
      "match": "prefix",
      "scope": "admin",
      "auth": "bearer",
      "requireChild": true,
      "template": "/api/admin/smtp-credentials/{username}",
      "note": "the bare prefix (no username) is deliberately NOT claimed, matching the original regex"
    },
    {
      "id": "admin-reindex",
      "method": "POST",
      "path": "/api/admin/reindex",
      "match": "exact",
      "scope": "admin",
      "auth": "bearer"
    },
    {
      "id": "admin-roles",
      "method": "GET",
      "path": "/api/roles",
      "match": "exact",
      "scope": "admin",
      "auth": "bearer",
      "note": "the role map is operator config (#425): who is on which queue is a deployment fact, not a mailbox view, so a read token cannot see it"
    },
    {
      "id": "admin-reconcile",
      "method": "POST",
      "path": "/api/admin/reconcile",
      "match": "exact",
      "scope": "admin",
      "auth": "bearer",
      "note": "read-only report, gated admin because it spans the whole estate"
    },
    {
      "id": "message-delete",
      "method": "DELETE",
      "path": "/api/messages/",
      "match": "prefix",
      "scope": "delete",
      "auth": "bearer",
      "exclude": "/attachments/",
      "template": "/api/messages/{id}",
      "note": "not the /attachments/ sub-path"
    },
    {
      "id": "messages-list",
      "method": "GET",
      "path": "/api/messages",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "lens needs a viewer and refuses direction (#403); under a session to= filters INSIDE the account boundary (#422); seenFor moves only the read-state projection key (#404)"
    },
    {
      "id": "search",
      "method": "GET",
      "path": "/api/search",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "mode fts|substr|semantic|hybrid; field pairs with substr; the view filters mirror messages-list exactly because they share one builder"
    },
    {
      "id": "recipients-recent",
      "method": "GET",
      "path": "/api/recipients/recent",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "a bound identity wins; an unbound BYO token must name viewer= or to=, never an estate dump"
    },
    {
      "id": "folders",
      "method": "GET",
      "path": "/api/folders",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "to= scopes the unread counts; a bound identity wins over it server-side"
    },
    {
      "id": "mobileconfig",
      "method": "GET",
      "path": "/api/mobileconfig",
      "match": "exact",
      "scope": "read",
      "auth": "bearer",
      "note": "per-user Apple profile; carries NO password"
    },
    {
      "id": "message-get",
      "method": "GET",
      "path": "/api/messages/",
      "match": "prefix",
      "scope": "read",
      "auth": "bearer",
      "template": "/api/messages/{id}",
      "note": "single message and its /attachments/{i} sub-route"
    },
    {
      "id": "thread-get",
      "method": "GET",
      "path": "/api/threads/",
      "match": "prefix",
      "scope": "read",
      "auth": "bearer",
      "template": "/api/threads/{id}"
    }
  ]
}
